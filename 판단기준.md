# 판단 기준

혼자 자기 것을 만들 때 이 장만 보면 된다. 도메인 단어는 없다 — 문장이 이해 안 되면 아래 "오늘 내린 결정"에서 실제 예를 본다.

## 2026-09-02 — 8주차 Day1

- 차단과 경고를 가르는 것은 "계산이 틀리는가"다. 틀린다 → 차단. 해석만 조심하면 된다 → 경고.
- 검증 규칙은 적게 만든다. 차단을 늘리면 아무것도 못 돌리고 결국 검증을 끄게 된다.
- 임계값에는 근거를 적는다. 근거 없는 임계값은 남의 것이다.
- 메시지에는 실제 숫자를 넣는다. "실패"는 판단 재료가 아니다. 몇 건인지, 기준이 몇인지가 있어야 판단할 수 있다.
- 결측은 오류일 수도 구조일 수도 있다. 구조면 채우지 않는다. 채우면 위조다.
- 검증은 만든 것으로 끝나지 않는다. 정상 파일을 일부러 망가뜨려 넣어 실제로 막히는지 눈으로 본다.
- 남이 만든 임계값(예: 최소 표본 1,000)을 그대로 쓰지 않는다. 내 도메인의 실제 규모에 맞춰 다시 정한다 — 연간 수백 건인 도메인에서 1,000을 요구하면 어떤 구간도 통과하지 못한다.

### 오늘 내린 결정

- **행 수 규칙 → 차단(50건 미만).** 정상 규모(240건)의 약 20% 밑이면 파일이 잘못 온 것으로 보고, 비율 계산 자체를 신뢰하지 않기로 했다.
- **필수 컬럼 규칙 → 차단(그레인 식별자·주지표 컬럼 없으면).** 이 둘이 없으면 무엇을 세는지도, 주지표를 계산하는 것도 원천적으로 불가능해서 해석으로 넘길 수 있는 문제가 아니라고 판단했다.
- **날짜 범위 규칙 → 경고(설정 기간을 벗어난 행이 있으면).** 테이블이 하나뿐이라 조인이 오염될 위험이 없고, 벗어난 행만 제외해도 나머지 집계는 유효하다고 판단해 차단 대신 경고로 뒀다.
- **경고가 하나도 뜨지 않았다.** 정상 데이터라 규칙 3건 전부 통과했고, 게이트 1은 "경고 없음. 검증 3건 전부 통과."라는 근거로 통과시켰다.

## 2026-09-02 — 8주차 Day2

- 같은 데이터라도 무엇을 하나로 세느냐(그레인)에 따라 다른 숫자가 나온다. 둘 다 맞는 숫자다. 다른 질문에 답할 뿐이다.
- 내가 안 정하면 도구가 정한다. 그리고 정했다고 말해주지 않는다. 빈칸을 남기지 말고, 남겼으면 무엇을 정했는지 물어서 확인한다.
- 한 번 정한 것은 파일(CLAUDE.md)에 박아둔다. 매번 다시 설명하지 않는다 — 새 대화를 열어 그 파일만으로 답이 나오는지 직접 테스트해서 확인한다.
- 되돌릴 수 있게 해두면(버전 관리) 과감하게 시킬 수 있다. 되돌릴 수 없으면 조심스러워지고, 조심스러우면 배우지 못한다.
- 안 맞으면 그레인 → 분모 → 필터 → 기간 순으로 의심한다.
- 앞 단계를 안 거치고 온 대상이 많으면 퍼널이 아니라 분류다. 분류를 퍼널이라 부르고 전환율을 내면 안 된다 — 위반 건수를 반드시 실제로 세어본다.
- 관측 기간이 다른 것을 누적값으로 비교하지 않는다. 최근 것이 낮은 건 성과 문제가 아니라 시간(관측 기간)의 문제일 수 있다 — 다만 이것도 실제로 확인해야 아는 것이지, 항상 그런 건 아니다.
- 교안·문서가 가정한 라이브러리 동작과 실제 설치된 버전의 동작이 다를 수 있다. 결과가 이상하면 소스·공식 문서로 직접 확인한다 — 짐작으로 "아마 이럴 것"이라고 넘기지 않는다.
- 테스트 도구 자체의 한계와 실제 코드의 결함을 혼동하지 않는다. 도구가 특정 동작을 재현 못 하면, 코드 조건을 직접 검토해 충족 여부를 판단하고 그 한계를 솔직히 밝힌다.

### 오늘 내린 결정

- **그레인 = 체크리스트 항목 1건.** 한 대상이 같은 단계를 두 번 밟지 않는 데이터라 "사람이냐 방문이냐" 같은 선택 자체가 없었다.
- **유지 정의 = 계정과목 기준, 문제월(승인율<50%) 연속 개월수로 3단계(1→2→3개월 연속).** 담당자 기준 대신 계정과목 기준을 선택했다(사용자 결정). 그레인이 계정과목 7개뿐이라 표본이 매우 작다는 한계를 명시했다.
- **지표 카드 4개 = 최종승인율(전환)·완료율(가드레일)·평균 지연일수(속도)·재작업률(품질).** 규모 축은 비웠다 — 이 도메인에 자연스러운 "규모" 지표(예: 월 신규 건수)를 넣을지는 아직 정하지 않았다.
- **완료율·평균 지연일수·재작업률 임계값은 최근 12개월 관측 최댓값/최솟값 근처로 잡았다.** 재작업률은 "근거 약함"으로 명시했다 — 실제 업무 부담 기준이 아직 없다.
- **MIN_SAMPLE 1,000 → 30.** 연간 240건인 도메인에서 1,000을 요구하면 어떤 구간도 통과하지 못한다.
- **유효 구간 = 전체 12개월(2026-01~12) 그대로 채택.** 최근 달(12월, 55.6%)이 오히려 평균 이상이라 "최근 것일수록 관측 시간이 부족해 낮게 나온다"는 편향의 증거가 없었다.

### ★ Claude가 말 안 하고 정한 것 중 잡아낸 것

- 완료여부·최종승인 판정을 문자열 `"Y"` 정확 일치로 판정했다 — 결측·다른 표기가 섞여 있었다면 분모가 달라졌을 것이다. 실측으로 결측 0%, Y/N만 있음을 확인해 안전을 검증했다.
- 병목(`is_bottleneck`) 판정에서 첫 단계(등록, step_rate가 NaN)를 후보에서 제외했다 — 안 뺐으면 NaN이 최솟값 비교에서 이상하게 걸렸을 것이다.
- `funnel_by()`의 두 번째 인자 자리(원래 다른 테이블이 들어갈 자리)에 테이블이 하나뿐이라 같은 테이블을 임시로 넣었다 — 아직 이 함수 자체가 스텁이라 지금은 값에 영향이 없지만, 실제로 구현할 때 이 자리를 다시 봐야 한다.
- `st.column_config.ProgressColumn`의 printf식 포맷(`"%.1f%%"`)이 설치된 Streamlit 버전에서 0~1 값을 자동으로 ×100 하지 않는다는 것을 소스 문서를 직접 읽어 확인하고, 내장 `"percent"` 포맷으로 바꿨다 — 교안이 가정한 구버전 동작과 실제 동작이 달랐다.

## 2026-09-05 — 8주차 Day3

- 무엇으로 쪼갤지에는 정답이 없다. 쪼개서 격차가 보이는가보다 **그 격차를 손을 쓸 수 있는가**가 더 중요하다. 못 바꾸는 축으로 쪼개면 격차를 찾아도 할 일이 없다.
- 못 믿을 조건은 여러 개일 수 있어도 분기는 하나로 둔다. 조건마다 분기를 따로 만들면 나중에 하나를 빠뜨린다.
- 계산해 놓고 화면에 안 그리는 것과, 못 믿으면 계산 자체를 하지 않는 것은 다르다. 값이 변수에 있으면 언젠가 리포트나 로그로 새어 나간다 — 손에 없어야 못 쓴다.
- 못 믿는 상태를 회색으로라도 보여주지 않는다. 사람은 경고가 아니라 숫자를 기억하고, 회의실에서 인용되는 것은 결국 그 숫자다.
- 표본이 적을 때는 비율의 소수점이나 신뢰구간을 억지로 내지 않는다. 실제 값과 변화량, "표본 부족"이라는 사실 자체가 더 정직한 보고다.
- 주지표만 보고 판단하면 안 된다. 무엇을 해서 주지표가 좋아졌더라도, 그 대가로 나빠진 것(가드레일)이 있는지 반드시 같이 본다 — 좋아 보이는데 좋지 않은 경우를 잡아내는 것이 가드레일의 존재 이유다.
- 색은 판정을 전달하는 유일한 신호여야 한다. 강조색을 장식(제목·테두리)에 쓰면, 진짜 위험할 때 아무도 알아보지 못한다.
- 화면 재구성(부분 재실행 조각)과 URL 상태 공유가 충돌하면, 상태를 공유해야 하는 값(필터)을 조각 밖으로 빼는 쪽을 우선한다 — 조각 안에서 상태를 바꾸면 주소창과 화면이 어긋난다.
- 이상한 값이 담긴 링크를 받아도 앱이 죽으면 안 된다. 모르는 값은 조용히 기본값으로 떨어뜨린다.

### 오늘 내린 결정

- **분해 축 → 담당자.** 격차(24.7%p)가 가장 크고, 모든 칸이 최소표본(30건)을 넘어 감춰지는 칸이 없다. 계정과목(격차 14.9%p)도 후보였지만 가장 작은 칸(매출채권 21건)이 표본 미달로 실제로 감춰졌다.
- **못 믿을 조건 → 표본 30건 미만 + 비교 기간 길이 20% 이상 불일치.** 실험이 없는 도메인이라 배정 공정성 대신 이 둘만 썼다. 관측 기간 자체가 짧아 판정을 못 한다는 조건은 뺐다 — 어제 이미 12개월 전체에서 관측 기간 편향의 증거가 없음을 실측으로 확인했기 때문이다.
- **판정 임계값 → 주지표(최종승인율) 5%p / 가드레일(완료율) 2%p.** 월별 변동성(승인율 표준편차 13.7%p, 완료율 4.3%p)을 반기 집계 기준으로 환산해 보수적으로 잡았다. 이 기준으로 실제 상반기·하반기 비교는 "효과 없음"(변화 1.5%p)으로 판정됐다 — 정직한 결과이지 실패가 아니다.
