# 판단 기준

혼자 자기 것을 만들 때 이 장만 보면 된다. 도메인 단어는 없다 — 문장이 이해 안 되면 아래 "오늘 내린 결정"에서 실제 예를 본다.

## 2026-09-02 — 8주차 Day1

- 차단과 경고를 가르는 것은 "계산이 틀리는가"다. 틀린다 → 차단. 해석만 조심하면 된다 → 경고.
- 검증 규칙은 적게 만든다. 차단을 늘리면 아무것도 못 돌리고 결국 검증을 끄게 된다.
- 임계값에는 근거를 적는다. 근거 없는 임계값은 남의 것이다.
- 메시지에는 실제 숫자를 넣는다. "실패"는 판단 재료가 아니다. 몇 건인지, 기준이 몇인지가 있어야 판단할 수 있다.
- 결측은 오류일 수도 구조일 수도 있다. 구조면 채우지 않는다. 채우면 위조다.
- 검증은 만든 것으로 끝나지 않는다. 정상 파일을 일부러 망가뜨려 넣어 실제로 막히는지 눈으로 본다.
- 남이 만든 임계값(예: 최소 표본 1,000)을 그대로 쓰지 않는다. 내 도메인의 실제 규모에 맞춰 다시 정한다 — 연간 수백 건인 도메인에서 1,000을 요구하면 어떤 구간도 통과하지 못한다.

### 오늘 내린 결정

- **행 수 규칙 → 차단(50건 미만).** 정상 규모(240건)의 약 20% 밑이면 파일이 잘못 온 것으로 보고, 비율 계산 자체를 신뢰하지 않기로 했다.
- **필수 컬럼 규칙 → 차단(그레인 식별자·주지표 컬럼 없으면).** 이 둘이 없으면 무엇을 세는지도, 주지표를 계산하는 것도 원천적으로 불가능해서 해석으로 넘길 수 있는 문제가 아니라고 판단했다.
- **날짜 범위 규칙 → 경고(설정 기간을 벗어난 행이 있으면).** 테이블이 하나뿐이라 조인이 오염될 위험이 없고, 벗어난 행만 제외해도 나머지 집계는 유효하다고 판단해 차단 대신 경고로 뒀다.
- **경고가 하나도 뜨지 않았다.** 정상 데이터라 규칙 3건 전부 통과했고, 게이트 1은 "경고 없음. 검증 3건 전부 통과."라는 근거로 통과시켰다.
