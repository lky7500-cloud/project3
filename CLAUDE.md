# CLAUDE.md

## 이 앱이 무엇인가

결산체크리스트 이행 퍼널(등록→완료→최종승인)의 승인 병목을 분석하는 리포트 앱.

## 그레인

체크리스트 항목 1건(체크ID). 한 행이 같은 단계를 두 번 밟지 않으므로 고유값 처리 없이 행을 그대로 센다.

## 퍼널 단계와 조건

| 단계 | 조건 |
|---|---|
| 등록 | 전체 행 |
| 완료 | `완료여부 == "Y"` |
| 최종승인 | `최종승인 == "Y"` |

## 지표 이름과 계산식

| 지표 | 계산식 | 상태 |
|---|---|---|
| 최종승인율(주지표) | 최종승인='Y' 건수 ÷ 전체 건수 | 확정 |
| 완료율(가드레일) | 완료여부='Y' 건수 ÷ 전체 건수 | 확정 |
| 평균 지연일수(속도) | 지연일수 컬럼 평균 | 확정 |
| 재작업률(품질) | 검토횟수>=3 건수 ÷ 전체 건수 | 확정 |

## 임계값과 근거

- 최종승인율: 경고 42.9% / 위험 32.9% (현재값 52.9% 대비 상대 하락 10%p/20%p — 최근 12개월 승인율 변동폭이 26.7~76.5%로 커서 절대선 대신 상대선 채택)
- 완료율: 경고 90% / 위험 85% (최근 12개월 최저치 88.2%보다 낮게 위험선을 잡아 실제로 안 벌어졌던 수준을 위험으로 봄)
- 평균 지연일수: 경고 2.0일 / 위험 3.0일 (최근 12개월 관측 최댓값 2.0일 부근을 경고로)
- 재작업률: 경고 55% / 위험 65% (최근 12개월 관측 최댓값 58.8% 부근을 경고로. 근거 약함 — 실제 업무 부담 기준 미확인)
- 최소표본(MIN_SAMPLE): 30건 (연간 240건 도메인에서 1,000은 어떤 구간도 못 채움)

## 유지 퍼널

그레인: 계정과목(7개). 월별 최종승인율 50% 미만을 "문제월"로 보고 최장 연속 개월수로 3단계(문제 발생→2개월 연속→3개월 연속=구조적 병목)를 판정한다. 실측: 7개 중 2개(부가세예수금·현금) 3개월 연속 해당. 계정과목×월 표본이 1~7건뿐이라 참고 신호로만 쓴다.

## 못 믿을 조건 (8주차 목요일)

1. 표본이 30건 미만(config.MIN_SAMPLE)
2. 비교하는 두 기간 길이가 20% 이상 다름
   - (실험이 없는 도메인이라 배정 공정성 대신 이 둘만 본다. `core/metrics.py trust_check()`)

## 판정 순서 (반기 비교 카드 · `half_year_comparison()`)

못 믿을 조건 → 주지표(최종승인율, 5%p 미만이면 "효과 없음") → 가드레일(완료율, 2%p 이상 나빠지면 "주의 필요") → 그 외 "성공"(나빠지는 쪽 5%p 이상이면 "악화"). 못 믿을 때는 계산하지 않는다(계산 후 숨기는 것이 아니다). 감춘 항목에는 사유만 적고 수치는 적지 않는다.

## 코드를 고칠 때 지킬 것

- 지표 정의는 `config.py`에서만 바꾼다.
- 계산 경로에 현재 시각을 넣지 않는다.
- 시키지 않은 파일은 고치지 않는다.
